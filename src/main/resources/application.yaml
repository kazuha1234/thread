server:
    port: 8080
    servlet:
        context-path: /thread

spring:
    datasource:
        url: jdbc:mysql://localhost:3306/thread
        username: root
        password: 492003
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: update
        show-sql: true
